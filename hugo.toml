baseURL = 'https://Diing54.github.io/devops/'
languageCode = 'en-us'
title = 'DevOps Brain'
theme = "github.com/imfing/hextra"

[module]
  [[module.imports]]
    path = "github.com/imfing/hextra"

[outputs]
  home = ["HTML", "RSS", "JSON"]

# This section fixes the "Raw HTML" warning
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[menu]
  [[menu.main]]
    identifier = "docs"
    name = "Notes"
    url = "/docs/"
    weight = 1

[params]
  footer = false 

  # FIXED: navbar is now a section, not a boolean
  [params.navbar]
    displayTitle = true
    displayLogo = true

  [params.search]
    enable = true
    type = "flexsearch"
    
  [params.editURL]
    enable = false
